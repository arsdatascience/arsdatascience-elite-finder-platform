# Adicione estas linhas ao seu arquivo .env no backend

# Chave Secreta do Stripe (Developers > API keys)
STRIPE_SECRET_KEY=sk_test_sua_chave_aqui

# Segredo do Webhook (Developers > Webhooks > Add endpoint ou Stripe CLI)
STRIPE_WEBHOOK_SECRET=whsec_seu_segredo_aqui

# IDs dos Preços (Products > Selecione o produto > Pricing > API ID)
STRIPE_PRICE_PRO=price_id_do_plano_pro
STRIPE_PRICE_ENTERPRISE=price_id_do_plano_enterprise

# URL do Frontend (para onde o usuário volta após pagar)
FRONTEND_URL=http://localhost:5173
